# -- Site Parameters --
# The slate layer is theme-less and reads only a handful of params. Author
# identity, description, and language metadata live in languages.en.toml
# [params]; Umami analytics is wired directly in layouts/_partials/head.html.

# Fallback Open Graph / Twitter share image, used by head.html when a page has
# no featured.* page-resource of its own.
defaultSocialImage = "img/profile.jpg"
